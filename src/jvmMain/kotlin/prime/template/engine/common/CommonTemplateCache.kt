package prime.template.engine.common

import prime.template.engine.Template
import java.util.*

class CommonTemplateCache {
  fun cacheTemplate(template: Optional<Template>): Optional<Template> {
    return template;
  }
}